{
  "name": "org-admin",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue,bgMagenta\" \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "cd backend && bun run dev",
    "dev:frontend": "cd frontend && bun run dev",
    "build": "bun run build:backend && bun run build:frontend",
    "build:backend": "cd backend && bun run build",
    "build:frontend": "cd frontend && bun run build",
    "start": "cd backend && NODE_ENV=production bun run start",
    "start:backend": "cd backend && bun run start",
    "lint": "bun run lint:backend && bun run lint:frontend",
    "lint:backend": "cd backend && bun run lint",
    "lint:frontend": "cd frontend && bun run lint",
    "lint:check": "bun run lint:check:backend && bun run lint:check:frontend",
    "lint:check:backend": "cd backend && bun run lint:check",
    "lint:check:frontend": "cd frontend && bun run lint:check"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
